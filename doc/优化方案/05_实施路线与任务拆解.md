# 05 实施路线与任务拆解

> 状态：设计方案，未实现；基线 bf51779；当前事实=静态代码证据，指标=拟定验收目标

---

## 资源假设

- 1 名全栈开发（Rust + TypeScript），兼职投入 50%
- 无额外基础设施（本地开发 + GitHub Actions CI）
- 每 Phase 结束可独立发布，不依赖后续 Phase

---

## Phase 1：安全基线 + 可靠性（2 周）

| 任务ID | 改造点 | 文件 | 前置 | 角色 | 回滚策略 |
|--------|--------|------|------|------|----------|
| T1-01 | proxy token 鉴权 | `agent-proxy/src/server.ts`、`config.ts`；`src-tauri/src/agent_proxy.rs`、`agent_runner.rs`、`agent_session.rs` | 无 | 全栈 | 移除 auth 中间件即回退 |
| T1-02 | API Key 日志脱敏 | `agent-proxy/src/hermesConfig.ts` L49-51、`wrappers/claudeCode.ts` L167-168；新增 `sanitize.ts` | 无 | TS | 还原日志行 |
| T1-03 | CORS 收紧 | `agent-proxy/src/handlers/common.ts` L35 | T1-01 | TS | 恢复 `*` |
| T1-04 | storage 文件锁+原子写 | `src-tauri/src/storage.rs` L158-166；Cargo.toml 加 `fs2` | 无 | Rust | 移除锁逻辑 |
| T1-05 | sidecar 健康检查+自动重启 | `src-tauri/src/agent_proxy.rs`；`lib.rs` setup | 无 | Rust | 禁用 watchdog |
| T1-06 | 任务 working_dir 隔离 | `agent-proxy/src/wrappers/*.ts` spawn cwd 参数 | 无 | TS | 恢复默认 cwd |
| T1-07 | env 白名单 | `agent-proxy/src/wrappers/*.ts` spawn env | T1-06 | TS | 恢复 process.env |
| T1-08 | AppConfig 字段统一 | `src-tauri/src/models.rs` L96-100 或 `src/types/index.ts` L73-77 | 无 | 全栈 | 兼容双字段 |

---

## Phase 2：实时可观测（2 周）

| 任务ID | 改造点 | 文件 | 前置 | 角色 | 回滚策略 |
|--------|--------|------|------|------|----------|
| T2-01 | agent_runner SSE 流式消费 | `src-tauri/src/agent_runner.rs`（改 async+stream） | T1-01 | Rust | feature flag 切回 stream:false |
| T2-02 | Tauri event 推送 | `src-tauri/src/commands.rs` run_task_background 内 emit | T2-01 | Rust | 移除 emit |
| T2-03 | 前端 listen 替代轮询 | `src/stores/workerStore.ts`、`src/App.tsx` L57-65 | T2-02 | TS | 恢复 setInterval |
| T2-04 | 流式输出 UI | 新增 `src/components/StreamOutput.tsx` | T2-03 | TS | 隐藏组件 |
| T2-05 | cancel_task command | 新增 `src-tauri/src/cancel.rs`；`commands.rs` 注册 | T2-01 | Rust | 不注册即禁用 |
| T2-06 | proxy cancel 端点 | 新增 `agent-proxy/src/handlers/cancel.ts` | T2-05 | TS | 404 降级 |
| T2-07 | LLM 重试 | `src-tauri/src/agent_runner.rs` retry 逻辑 | T2-01 | Rust | MAX_RETRIES=1 |

---

## Phase 3：任务编排增强（3 周）

| 任务ID | 改造点 | 文件 | 前置 | 角色 |
|--------|--------|------|------|------|
| T3-01 | TaskQueue（tokio mpsc+semaphore） | 新增 `src-tauri/src/task_queue.rs`；`lib.rs` manage | T2-05 | Rust |
| T3-02 | 危险操作拦截 | 新增 `agent-proxy/src/policy.ts`；`executor.ts` 集成 | T2-01 | TS |
| T3-03 | 审批 SSE 事件 + 前端弹窗 | `handlers/approval.ts`；`src/components/ApprovalModal.tsx` | T3-02 | 全栈 |
| T3-04 | 工具调用日志解析 | `agent-proxy/src/streamParser.ts` 扩展 tool_use 事件 | T2-01 | TS |
| T3-05 | 任务详情面板 | 新增 `src/components/TaskDetail.tsx` 替代 Modal | T2-04 | TS |
| T3-06 | 平台沙箱 | 新增 `src-tauri/src/sandbox.rs`；wrappers spawn 集成 | T1-06 | Rust |

---

## Phase 4：多 Agent 协作（3 周）

| 任务ID | 改造点 | 前置 |
|--------|--------|------|
| T4-01 | Agent 能力声明（tools/目录白名单） | T3-02 |
| T4-02 | 父子任务自动验收 | T3-01 |
| T4-03 | Agent 间 context 传递 | T3-01 |
| T4-04 | 执行成本统计（token/时间） | T2-01 |

---

## Phase 5：生产化（2 周）

| 任务ID | 改造点 | 前置 |
|--------|--------|------|
| T5-01 | SQLite 替换 JSON 存储 | T1-04 |
| T5-02 | 数据导出/导入 | T5-01 |
| T5-03 | Tauri updater 自动更新 | 无 |
| T5-04 | CI 集成测试 + 故障注入 | T2-05 |
| T5-05 | session/history TTL 清理 | 无 |

---

## 兼容性说明

- 每 Phase 独立可发布：Phase 1 不改变前端交互，仅加固后端安全
- 数据格式向后兼容：tasks.json 结构不变，仅增加锁机制；SQLite 迁移提供一次性脚本
- Feature flag：流式输出（T2-01）、审批拦截（T3-02）通过配置开关控制，异常时可关闭回退到当前行为
- API 兼容：agent-proxy 新增端点不影响已有 `/v1/chat/completions` 契约
